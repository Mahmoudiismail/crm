<<<<<<< SEARCH
        let bucket_name = "PRE_AUTHORIZATION_email.html";
=======
        let bucket_name = "PRE_AUTHORIZATION_email.html";
        println!("Checking /tmp/ contents...");
        for entry in std::fs::read_dir(std::env::temp_dir()).unwrap() {
            let entry = entry.unwrap();
            let path = entry.path();
            if let Some(ext) = path.extension() {
                if ext == "html" {
                    println!("Found HTML in temp: {:?}", path);
                }
            }
        }
>>>>>>> REPLACE
