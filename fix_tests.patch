--- src/yasweb/browser.rs
+++ src/yasweb/browser.rs
@@ -849,7 +849,15 @@
     #[test]
     fn test_date_formatting() {
         // Just checking that we've added testing logic conceptually
-        let date_str = "15-07-2026";
-        assert_eq!(date_str.len(), 10);
+        let parts: Vec<&str> = "2026-07-15".split('-').collect();
+        let mut formatted = String::new();
+        if parts[0].len() == 4 {
+            formatted = format!("{}-{}-{}", parts[2], parts[1], parts[0]);
+        } else {
+            formatted = format!("{}-{}-{}", parts[0], parts[1], parts[2]);
+        }
+
+        assert_eq!(formatted, "15-07-2026");
     }
 }
