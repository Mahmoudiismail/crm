--- src/runner/engine.rs
+++ src/runner/engine.rs
@@ -628,7 +628,7 @@
     filters: &std::collections::HashMap<String, String>,
     timeout_seconds: u64,
 ) -> Result<()> {
-    let resolved_executable = resolve_crm_executable(executable_path);
+    let resolved_executable = resolve_executable(executable_path);
     let resolved_config_path = resolve_relative_to_exe_dir(config_path);

     let mut command = tokio::process::Command::new(&resolved_executable);
@@ -1071,7 +1071,7 @@
     timeout_seconds: u64,
 ) -> Result<()> {
-    let resolved_executable = resolve_crm_executable(executable_path);
+    let resolved_executable = resolve_executable(executable_path);
     let resolved_config_path = resolve_relative_to_exe_dir(config_path);

     let mut command = tokio::process::Command::new(&resolved_executable);
@@ -1113,7 +1113,7 @@
 }

-fn resolve_crm_executable(configured_name: &str) -> PathBuf {
+fn resolve_executable(configured_name: &str) -> PathBuf {
     let candidate = PathBuf::from(&configured_name);
     if candidate.is_absolute() {
         return candidate;
