services:
  # ── Dev container (for coding, building, debugging) ──
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: crm-rust-dev
    volumes:
      - .:/workspace
      - cargo-registry:/root/.cargo/registry
      - cargo-git:/root/.cargo/git
      - target-cache:/workspace/target
    working_dir: /workspace
    stdin_open: true
    tty: true
    command: sleep infinity

  # ── One-shot: build Windows .exe ──
  build-windows:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/workspace
      - cargo-registry:/root/.cargo/registry
      - cargo-git:/root/.cargo/git
      - target-cache:/workspace/target
    working_dir: /workspace
    command: >
      bash -c "
        echo '══════════════════════════════════════════'
        echo ' Building Windows executables'
        echo '══════════════════════════════════════════'
        cargo build --release --target x86_64-pc-windows-gnu 2>&1
        STATUS=$$?
        if [ $$STATUS -eq 0 ]; then
          cp target/x86_64-pc-windows-gnu/release/runner.exe /workspace/runner.exe
          cp target/x86_64-pc-windows-gnu/release/crm.exe /workspace/crm.exe
          echo ''
          echo '✓ Windows binaries: ./runner.exe and ./crm.exe'
          ls -lh /workspace/runner.exe /workspace/crm.exe
        else
          echo '✗ Build failed'
          exit 1
        fi
      "

volumes:
  cargo-registry:
  cargo-git:
  target-cache:
